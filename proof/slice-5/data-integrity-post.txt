=== token_usage row count, sums ===
47|69136|9.5222847

=== token_usage schema ===
CREATE TABLE token_usage (
      id              INTEGER PRIMARY KEY AUTOINCREMENT,
      chat_id         TEXT NOT NULL,
      session_id      TEXT,
      input_tokens    INTEGER NOT NULL DEFAULT 0,
      output_tokens   INTEGER NOT NULL DEFAULT 0,
      cache_read      INTEGER NOT NULL DEFAULT 0,
      context_tokens  INTEGER NOT NULL DEFAULT 0,
      cost_usd        REAL NOT NULL DEFAULT 0,
      did_compact     INTEGER NOT NULL DEFAULT 0,
      created_at      INTEGER NOT NULL
    , agent_id TEXT NOT NULL DEFAULT 'main');
CREATE INDEX idx_token_usage_session ON token_usage(session_id, created_at);
CREATE INDEX idx_token_usage_chat ON token_usage(chat_id, created_at DESC);

=== mission_tasks schema ===
CREATE TABLE mission_tasks (
      id              TEXT PRIMARY KEY,
      title           TEXT NOT NULL,
      prompt          TEXT NOT NULL,
      assigned_agent  TEXT,
      status          TEXT NOT NULL DEFAULT 'queued',
      result          TEXT,
      error           TEXT,
      created_by      TEXT NOT NULL DEFAULT 'dashboard',
      priority        INTEGER NOT NULL DEFAULT 0,
      created_at      INTEGER NOT NULL,
      started_at      INTEGER,
      completed_at    INTEGER
    );
CREATE INDEX idx_mission_status
      ON mission_tasks(assigned_agent, status, priority DESC, created_at ASC);

=== distinct agent_ids in token_usage ===
main|41
research|3
goldbot-labs|3

=== mission_tasks status counts ===
comms|completed|2
main|completed|2

=== convolife with most-recent populated session 9363cf72 ===
3|44653|7994|0.6017412|0
